# Accessibility
Use semantic landmarks, one clear h1, ordered headings, keyboard-operable controls, visible `:focus-visible`, descriptive alt text and WCAG AA contrast. Buttons must be buttons and links must navigate. Respect reduced motion.
