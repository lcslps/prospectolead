# Animation
Motion supports hierarchy. Prefer one short reveal language and subtle hover feedback, with consistent easing and duration. Avoid continuous, heavy or decorative movement. Implement `prefers-reduced-motion` so content remains complete without animation.
