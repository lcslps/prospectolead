# Images
Use only supplied asset tokens or declared intent tokens. Real business photography has priority and must not be mislabeled. Give every image an explicit aspect ratio, object position, meaningful alt text, width/height where known, and lazy loading below the fold. Never invent a URL.
